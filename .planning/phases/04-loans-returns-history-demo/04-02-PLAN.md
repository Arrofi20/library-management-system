---
phase: 04-loans-returns-history-demo
plan: 02
type: standard
wave: 2
depends_on:
  - "01"
files_modified:
  - client/src/App.jsx
  - client/src/components/AppShell.jsx
  - client/src/components/LoansPage.jsx
  - client/src/components/LoanRow.jsx
  - client/src/components/MyLoansPage.jsx
  - client/src/api/loans.js
  - server/src/app.js
  - server/src/routes/loans.js
  - server/tests/loans.test.js
  - client/src/__tests__/auth-flow.test.jsx
autonomous: true
requirements:
  - LOAN-05
  - LOAN-06
  - DEMO-01
must_haves:
  truths:
    - "Librarians can view a single Loans page with active loans and loan history in one place."
    - "The Loans page supports filtering history by member and by book for demo review."
    - "Students can see their full loan timeline, including returned loans, without losing access to active loan details."
    - "Course-demo wording and navigation clearly guide both student and librarian workflows."
  artifacts:
    - path: "client/src/components/LoansPage.jsx"
      provides: "Librarian Loans page with active loans, return actions, and loan history sections."
      min_lines: 140
    - path: "client/src/components/LoanRow.jsx"
      provides: "Reusable loan row component that shows status badges and actions for active loans."
      min_lines: 80
    - path: "client/src/api/loans.js"
      provides: "Loan API helpers for query and return operations used by both student and librarian pages."
      min_lines: 40
  key_links:
    - from: "client/src/components/LoansPage.jsx"
      to: "GET /api/loans/active"
      via: "Fetch librarian active loans and loan history for the combined page."
    - from: "client/src/components/LoansPage.jsx"
      to: "PUT /api/loans/:id/return"
      via: "Allow librarians to return active loans inline from the table."
    - from: "server/src/routes/loans.js"
      to: "server/src/db.js"
      via: "Use the loan return endpoint to keep book availability and loan history consistent."

<objective>
Build the librarian loan review page, history filters, and demo polish so the final borrow lifecycle is easy to present and verify.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-loans-returns-history-demo/04-CONTEXT.md
@.planning/phases/03-member-management-and-borrow-requests/03-CONTEXT.md
</context>

<phase_goal>
As a librarian, I can review active loans, record returns, and inspect loan history by member or book, while students can view their complete loan timeline.
</phase_goal>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create the librarian Loans page with combined active loans and history sections</name>
  <files>client/src/App.jsx, client/src/AppShell.jsx, client/src/components/LoansPage.jsx, client/src/components/LoanRow.jsx, client/src/api/loans.js, client/src/styles.css</files>
  <behavior>
    - The librarian sidebar includes a `Loans` navigation item.
    - The page shows active loans first and returned history below.
    - Librarians can search or filter history by member name or book title.
    - Active loans include a `Return` button for inline completion.
  </behavior>
  <action>Implement `LoansPage` and `LoanRow` components using existing table and form patterns. Add librarian navigation to `AppShell` and load the page from `App.jsx`. Reuse `client/src/api/loans.js` helpers for data access.</action>
  <verify>
    <automated>npm run test --workspace client -- auth-flow.test.jsx</automated>
  </verify>
  <done>The librarian Loans page is functional, with active loans, return actions, and history display available in one place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loan history detail and filtering for students and librarians</name>
  <files>client/src/components/LoansPage.jsx, client/src/components/MyLoansPage.jsx, client/src/api/loans.js, server/src/routes/loans.js, server/tests/loans.test.js</files>
  <behavior>
    - Returned loans include `returned_at` and status badges.
    - History rows show loan start date, returned date, book title, and borrower/member name.
    - Librarians can filter history by member or book to support demo review.
    - Students can view their own loan timeline without extra navigation complexity.
  </behavior>
  <action>Extend loan query responses to include the metadata needed for history display. Add client-side filtering controls on the librarian Loans page and ensure the My Loans page renders returned loan detail clearly.</action>
  <verify>
    <automated>npm test --workspace server -- loans.test.js</automated>
  </verify>
  <done>Both student and librarian loan history views show complete timeline detail and allow simple review/filtering for demo presentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Polish the loan workflows for course-demo readiness</name>
  <files>client/src/App.jsx, client/src/components/AppShell.jsx, client/src/components/LoansPage.jsx, client/src/components/MyLoansPage.jsx, client/src/components/CatalogPage.jsx, client/src/api/loans.js, server/src/routes/loans.js</files>
  <behavior>
    - Navigation labels are clear for student and librarian workflows.
    - Return actions and loan status changes produce concise success/failure messaging.
    - Demo flows are complete: student request -> librarian approval -> active loan -> return -> history.
    - UI copy remains utilitarian and consistent with existing alerts and table patterns.
  </behavior>
  <action>Review the student and librarian loan pages, refine alerts/messages, ensure page navigation is obvious, and confirm the end-to-end demo path is smooth. Fix any UX rough edges that would confuse a course demonstration.</action>
  <verify>
    <automated>npm run test --workspace client -- auth-flow.test.jsx</automated>
  </verify>
  <done>The loan lifecycle is polished for demo use, with clear user pathways and consistent app behavior for both roles.</done>
</task>
</tasks>

<verification>
Verify the combined loan workflows and demo polish with these checks:
1. `npm run test --workspace client -- auth-flow.test.jsx`
2. Librarian login -> Loans page shows active loans and history.
3. Student login -> My Loans page shows active and returned loans.
4. Return a loan and verify the book becomes available in the catalog and the loan appears in history.
</verification>

<success_criteria>
- LOAN-05, LOAN-06, and DEMO-01 are covered by the plan.
- The librarian Loans page supports review by member and by book.
- The final borrow lifecycle is easy to demonstrate from the app's navigation.
</success_criteria>

<threat_model>
| Threat | STRIDE | Disposition | Mitigation |
|---|---|---|---|
| Confusing loan history state | Information disclosure | mitigate | Show clear returned status and timestamps in both student and librarian views.
| Return action on the wrong loan | Tampering | mitigate | Only show `Return` on active loans and guard the endpoint server-side.
| Demo workflow failure due to stale navigation | Tampering | mitigate | Keep loan navigation consistent and test the full end-to-end flow manually.
</threat_model>
