---
phase: 03-member-management-and-borrow-requests
plan: 01
type: standard
wave: 1
depends_on: []
files_modified:
  - client/src/App.jsx
  - client/src/components/AppShell.jsx
  - client/src/components/MembersPage.jsx
  - client/src/components/MemberForm.jsx
  - client/src/api/members.js
  - server/src/app.js
  - server/src/db.js
  - server/src/routes/members.js
  - server/tests/members.test.js
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
must_haves:
  truths:
    - "Librarians can create, view, edit, and deactivate student member records from a single Members page."
    - "Member deactivation blocks new borrow requests but does not remove the member's login or existing request/loan history."
    - "The member management UI mirrors the existing Books page pattern with a table and inline add/edit flow."
    - "API routes are protected by `requireRole(\"librarian\")` for write actions and return clear JSON errors on invalid input."
  artifacts:
    - path: "server/src/routes/members.js"
      provides: "Member API routes for list, create, update, and deactivate actions."
      min_lines: 120
    - path: "client/src/components/MembersPage.jsx"
      provides: "Librarian member management UI with table, add/edit form, and deactivate control."
      min_lines: 120
    - path: "client/src/api/members.js"
      provides: "Client-side member API helpers that match the existing auth/fetch pattern."
      min_lines: 40
  key_links:
    - from: "server/src/routes/members.js"
      to: "server/src/auth/guards.js"
      via: "Use librarian guard middleware for create/update/deactivate routes."
    - from: "client/src/components/MembersPage.jsx"
      to: "POST /api/members"
      via: "Create new member records through librarian-only API."
    - from: "client/src/components/MembersPage.jsx"
      to: "PUT /api/members/:id"
      via: "Edit existing member details and deactivate status."

<objective>
Implement librarian member management: member schema, read/write API, and the librarian Members page. The page should preserve the existing Books page shape while using the student account model for member records.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-member-management-and-borrow-requests/03-CONTEXT.md
@.planning/phases/03-member-management-and-borrow-requests/03-RESEARCH.md
</context>

<phase_goal>
As a librarian, I can manage student member records so future borrow requests can be assigned to valid members.
</phase_goal>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the database schema for member status and borrow eligibility</name>
  <files>server/src/db.js, server/src/seed.js, server/tests/members.test.js</files>
  <behavior>
    - `users` includes a `member_active` or `can_borrow` boolean flag.
    - Existing student accounts remain valid for login while member deactivation blocks new borrow requests.
    - The seed data includes at least one active student member and one librarian. 
  </behavior>
  <action>Update `server/src/db.js` to add a member status flag on the user table. Preserve the existing `users` account model and add any helper functions required to query member eligibility. Update seed data as needed so member status is demonstrable in tests.</action>
  <verify>
    <automated>npm test --workspace server -- members.test.js</automated>
  </verify>
  <done>Server tests confirm member records can be persisted with active/inactive status and the status flag is queryable for eligibility checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add librarian member management API routes</name>
  <files>server/src/routes/members.js, server/src/app.js, server/src/auth/guards.js, server/tests/members.test.js</files>
  <behavior>
    - `GET /api/members` returns librarian-only member list data.
    - `POST /api/members` creates a new student/member record with required fields.
    - `PUT /api/members/:id` updates editable member fields and toggles deactivation.
    - Member creation and updates are protected by `requireRole("librarian")`.
  </behavior>
  <action>Create a modular `members` router and mount it in `server/src/app.js` under `/api/members`. Reuse guards for librarian-only write routes. Validate required identity fields and return consistent JSON error messages for missing or duplicate values.</action>
  <verify>
    <automated>npm test --workspace server -- members.test.js</automated>
  </verify>
  <done>Automated tests prove the member API supports list/create/update/deactivate flows and enforces librarian-only access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the librarian Members page with table and inline add/edit form</name>
  <files>client/src/App.jsx, client/src/components/AppShell.jsx, client/src/components/MembersPage.jsx, client/src/components/MemberForm.jsx, client/src/api/members.js, client/src/styles.css, client/src/__tests__/auth-flow.test.jsx</files>
  <behavior>
    - Librarians see a Members page entry in navigation.
    - The Members page shows a table of student members with name, email, role, member ID, and active status.
    - A form allows adding new members and editing existing records.
    - Deactivation is available from the table or edit flow and does not remove the member from the list.
  </behavior>
  <action>Implement `MembersPage` and a shared `MemberForm` component to mirror the existing Books page CRUD pattern. Use `client/src/api/members.js` to fetch list data and submit create/update actions. Keep UI copy concise and match the app's current utilitarian styling.</action>
  <verify>
    <automated>npm run test --workspace client -- auth-flow.test.jsx</automated>
  </verify>
  <done>Frontend tests or smoke validation confirm the librarian can view, add, edit, and deactivate member records from the Members page.</done>
</task>
</tasks>

<verification>
Run backend member tests first, then update the client and manually verify:
1. `npm test --workspace server -- members.test.js`
2. `npm run test --workspace client -- auth-flow.test.jsx`
3. Librarian login -> Members page loads with active/inactive student rows.
4. Add a new member, edit an existing member, and toggle deactivation without affecting login ability.
</verification>

<success_criteria>
- MEM-01 through MEM-04 are covered by the API and UI behavior.
- Member deactivation blocks borrowing eligibility without removing account access.
- The librarian Members page uses the existing Books page pattern and maintains course-demo simplicity.
</success_criteria>

<threat_model>
| Threat | STRIDE | Disposition | Mitigation |
|---|---|---|---|
| Unauthorized member creation or edits | Elevation of privilege | mitigate | Protect member write routes with librarian guard middleware. |
| Deactivated members still able to request books | Tampering | mitigate | Ensure `member_active` is checked at request submission and not only at UI level. |
| Confusing member list state | Information disclosure | mitigate | Show explicit active/inactive status and keep deactivated members visible in the list. |
</threat_model>
