---
phase: 01-application-foundation-and-role-login
plan: 01
type: standard
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - client/package.json
  - client/index.html
  - client/src/main.jsx
  - client/src/App.jsx
  - client/src/styles.css
  - server/package.json
  - server/src/app.js
  - server/src/auth/passwords.js
  - server/src/db.js
  - server/src/seed.js
  - server/tests/smoke.test.js
autonomous: true
requirements:
  - DEMO-02
  - DEMO-03
  - DEMO-04
must_haves:
  truths:
    - "A developer can install dependencies and run the React and Express app locally with root scripts."
    - "The Express API exposes a real health/status endpoint backed by a SQLite connection."
    - "Seed/reset tooling creates demo users and books for later authentication and catalog slices."
  artifacts:
    - path: "package.json"
      provides: "Root install, dev, test, and seed scripts"
      min_lines: 20
    - path: "client/src/App.jsx"
      provides: "Initial React app shell that calls the Express API"
      min_lines: 40
    - path: "server/src/app.js"
      provides: "Express app with health/status route and JSON middleware"
      min_lines: 50
    - path: "server/src/auth/passwords.js"
      provides: "Password hashing helper used by seed data and auth routes"
      min_lines: 30
    - path: "server/src/db.js"
      provides: "SQLite connection helper and initialization boundary"
      min_lines: 40
    - path: "server/src/seed.js"
      provides: "Reset/reseed command with demo users and sample books"
      min_lines: 60
  key_links:
    - from: "client/src/App.jsx"
      to: "GET /api/health"
      via: "fetch call during app load renders API/database status"
    - from: "server/src/app.js"
      to: "server/src/db.js"
      via: "health route checks SQLite connection through shared db helper"
    - from: "package.json"
      to: "client/package.json and server/package.json"
      via: "root scripts delegate to workspace/client/server commands"
---

<objective>
Create the walking skeleton for the Library Management System: a Vite React client, Express API, SQLite connection, seed/reset command, and smoke tests that prove the full stack can run locally.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-application-foundation-and-role-login/01-CONTEXT.md
@.planning/phases/01-application-foundation-and-role-login/01-SKELETON.md
</context>

<phase_goal>
**As a** student or librarian, **I want to** open a database-backed app shell, **so that** later role workflows can build on a proven React, Express, and SQLite foundation.
</phase_goal>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add root, client, and server project scaffolds</name>
  <files>package.json, README.md, client/package.json, client/index.html, client/src/main.jsx, client/src/App.jsx, client/src/styles.css, server/package.json, server/src/app.js</files>
  <behavior>
    - Root `npm install` installs client and server dependencies.
    - Root `npm run dev` starts both frontend and backend development servers.
    - React renders a course-demo app shell rather than a landing page.
  </behavior>
  <action>Create root scripts for install/dev/test/seed. Scaffold a Vite React client under `client/` and an Express server under `server/`. Keep the UI utilitarian and app-first: header, main status area, and clear placeholders for authentication entry points. Do not implement catalog, borrowing, loans, or admin flows in this plan.</action>
  <verify>
    <automated>npm install && npm run test --workspace server</automated>
  </verify>
  <done>Root scripts exist, the React app renders, the Express app can be imported by tests, and README documents install/dev/test/seed commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SQLite initialization and health status slice</name>
  <files>server/src/db.js, server/src/app.js, server/tests/smoke.test.js, client/src/App.jsx</files>
  <behavior>
    - `GET /api/health` returns `ok: true`.
    - The response includes database availability from a real SQLite query.
    - React displays whether the API and database are reachable.
  </behavior>
  <action>Create `server/src/db.js` with a SQLite connection helper, schema initialization function, and safe query helpers using parameterized statements. Add `GET /api/health` to call the db helper. Add a server smoke test for the endpoint. Update the React shell to fetch `/api/health` through the Vite dev proxy and render loading, success, and failure states.</action>
  <verify>
    <automated>npm run test --workspace server</automated>
  </verify>
  <done>The smoke test proves Express can query SQLite, and the React app renders the live API/database status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add repeatable demo seed and reset command</name>
  <files>server/src/db.js, server/src/auth/passwords.js, server/src/seed.js, server/tests/smoke.test.js, package.json, README.md</files>
  <behavior>
    - `npm run seed` resets demo data.
    - Seed data includes one student, one librarian, and a few sample books.
    - Re-running seed is deterministic and does not duplicate records.
  </behavior>
  <action>Extend schema initialization for `users`, `sessions`, and `books` tables. Add a small `server/src/auth/passwords.js` helper backed by a standard bcrypt package so seeded credentials are never stored as plaintext. Implement `server/src/seed.js` to reset and seed exactly one librarian account, one student account, and sample books for demo readiness. Document seeded credentials in README and make the command idempotent.</action>
  <verify>
    <automated>npm run seed && npm run test --workspace server</automated>
  </verify>
  <done>The seed command can be run before a demo and leaves SQLite with the required demo accounts and sample books.</done>
</task>
</tasks>

<verification>
Run `npm install`, `npm run seed`, `npm run test --workspace server`, and `npm run dev`. Confirm the client renders API/database status from the Express/SQLite backend.
</verification>

<success_criteria>
- DEMO-02 is covered by a runnable Express API.
- DEMO-03 is covered by SQLite-backed health and seed behavior.
- DEMO-04 is covered by deterministic demo seed/reset data.
- Later plans can add authentication without changing the client/server directory boundaries.
</success_criteria>

<threat_model>
| Threat | STRIDE | Disposition | Mitigation |
|---|---|---|---|
| SQL injection in setup routes | Tampering | mitigate | Use parameterized SQLite statements in `server/src/db.js`. |
| Accidental demo credential exposure beyond local course demo | Information disclosure | accept | Demo hints are intentional for this local course project and scoped to seeded accounts only. |
| Seed command destroying non-demo data | Denial of service | mitigate | Document local-only reset semantics and keep database path project-local. |
</threat_model>
